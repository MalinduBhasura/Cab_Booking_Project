package com.cab_booking.controller;

import java.io.IOException;
import java.util.List;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.cab_booking.model.Booking;
import com.cab_booking.service.BookingService;

/**
 * Servlet implementation class AdminController
 */
/*
 * @WebServlet("/admin/bookings") public class AdminController extends
 * HttpServlet {
 *//**
	* 
	*//*
		 * private static final long serialVersionUID = 1L; private BookingService
		 * bookingService = new BookingService();
		 * 
		 * @Override protected void doGet(HttpServletRequest request,
		 * HttpServletResponse response) throws ServletException, IOException {
		 * List<Booking> bookings = bookingService.getAllBookings();
		 * request.setAttribute("bookings", bookings); RequestDispatcher dispatcher =
		 * request.getRequestDispatcher("/admin/viewBookings.jsp");
		 * dispatcher.forward(request, response); } }
		 */
